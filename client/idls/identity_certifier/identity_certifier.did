type Response = 
 variant {
   Expired;
   InvalidSession;
   NotConfirmed;
   Ok;
   Unauthorized;
 };
type NanosecondsTimeStamp = nat;
type IcIdentityVerifier = 
 service {
   confirmIdentity: (sessionId: text) -> (Response);
   documentation: () -> (text) query;
   initiateAuth: (sessionId: text, expirationNanoseconds:
    opt NanosecondsTimeStamp) -> (Response);
   verifyIdentity: (sessionId: text) -> (Response, opt principal);
 };
service : (admin: principal) -> IcIdentityVerifier
